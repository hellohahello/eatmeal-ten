spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    password: 1291482971a
    username: root
    url: jdbc:mysql://127.0.0.1:3306/selldb?characterEncoding=utf-8&useSSl=false&serverTimezone=UTC
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL5Dialect
server:
  port: 8080
  context-path: /sell